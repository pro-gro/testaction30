name: shell commands

on:
  - push

jobs:
  run-shell-command:
    
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: echo string
        run: echo "Hello World"

      - name: multi string
        run: |
          node -v
          npm -v